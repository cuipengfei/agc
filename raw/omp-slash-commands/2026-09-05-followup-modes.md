{"text": "# OMP v18.1.10 内置 slash 命令遗留细节补查（3 题）\n\n## #13 /fast：哪些 provider 的哪些模型支持 priority/fast service tier？\n\n`/fast` 的生效粒度是 **service-tier family**，不是单个模型 ID。`ModelControls.setFastMode` 先取当前模型的 family，再在该 family 上置 `priority` 标记（`session/model-controls.ts:745-764`）。\n\n**Family 判定代码** (`@oh-my-pi/pi-ai/src/types.ts:192-204`)：\n- `openai` family — provider 为 `openai`、`openai-codex`；或 OpenRouter 且 `identity.class === \"openai\"`；或自定义 relay 满足 api 为 `openai-completions`/`openai-responses`/`openai-codex-responses` 且 `identity.class === \"openai\"`（排除 `fireworks`、`github-copilot`）。\n- `anthropic` family — provider 为 `anthropic`；或 api 为 `anthropic-messages`（即 Bedrock/Vertex Claude）；或 OpenRouter 且 `identity.class === \"anthropic\"`。\n- `google` family — provider 为 `google`、`google-vertex`；或 OpenRouter 且 `identity.class === \"gemini\"`。\n- Fireworks 有独立的 `providers.fireworksTier` 设置，**不属于 `/fast` family 控制**。\n\n**但“设为 priority”不代表所有 family 都会真正生效**。`realizesPriorityServiceTier` (`@oh-my-pi/pi-ai/src/types.ts:258-267`) 对 `priority` 的 wire 实现判定：\n- `anthropic` provider：`true`（通过 `speed: \"fast\"`）。\n- OpenRouter 的 `openai` / `google` family：`true`。\n- OpenRouter 的 `anthropic` family：`false`。\n- api 为 `anthropic-messages` 的 Bedrock/Vertex Claude：`false`（priority 会被静默丢弃）。\n\n**结论**：`/fast` 对当前模型“可用”的前提是模型属于 `openai`/`anthropic`/`google` family；真正在请求中生效 priority 的目前只有 **OpenAI 直连接口、OpenRouter 的 OpenAI/Google 模型、Anthropic 直连**。Bedrock/Vertex Claude 与 OpenRouter 上的 Anthropic 模型虽能被归入 family，但 priority 不会 realized。\n\n---\n\n## #16 /cleanse：checker 发现与诊断收集是否使用 LSP？还是只跑 shell 命令？\n\n**诊断收集完全不依赖 LSP，只跑 shell 命令。**\n\n1. **Checker 发现有两条路**：\n   - **内置启发式发现** (`cleanse/checkers.ts:55`)：按项目文件类型扫描可执行文件（如 `eslint`、`tsc --noEmit`、`flake8`、`cargo check` 等），直接生成 `CheckerPlan`，全程无 LSP。\n   - **用户描述请求时的 discovery agent** (`cleanse/agent.ts:102-115`)：当用户传入自由文本请求（如 `\"ts errors\"`），启动一个结构化子 agent（`agent: \"task\"`），该 agent 会话虽开启 `enableLsp: true`，但其唯一任务是根据 prompt 返回可运行的 checker shell 命令列表（JSON schema：`{ checkers: [{ label, command: string[], parser? }] }`）。LSP 不是诊断来源，只是 agent 可选工具之一。\n\n2. **诊断收集** (`cleanse/checkers.ts:createSuite.run`)：对选中的 checker 直接 `runChecker`（即 spawn 子进程执行 shell 命令），通过 parser（如 `eslint`、`tsc`、`generic` 等）解析 stdout/stderr 得到 `CleanseDiagnostic`。没有任何 LSP 请求参与诊断采集。\n\n3. **修复阶段才涉及 LSP**：`dispatchWorker` (`cleanse/agent.ts:148`) 启动的 repair 子 agent 角色为 `sonic`，其会话 `enableLsp: true`，因此修复时可以用 LSP 辅助改代码，但这与“checker 发现/诊断收集”无关。\n\n**结论**：checker 发现与诊断收集均 **不使用 LSP**，走的是 **shell 命令（lint/typecheck/test）+ 输出解析**。\n\n---\n\n## #27 /security：扫什么对象？哪个 agent 执行？\n\n**扫描对象**：默认是当前 working directory 所在的 **Git 仓库**（`kind: \"repository\"`）。\n- `SecurityTargetRequest` 支持四种范围 (`security/preflight.ts:13-19`)：`repository`（默认）、`scoped_path`（`--path` 限定）、`ref_diff`（`--diff` 对比两 revision）、`working_tree`（`--working-tree`）。\n- 无论哪种，目标都被解析为 repository root 下的相对路径，通过 `digestWorkingTree` 或 `diffTree` 计算 SHA-256 tree digest，确保计划不可变。\n\n**执行 agent**：**两层子 agent 架构**。\n1. **Coordinator（主扫描会话）**：`createDefaultSecuritySession` (`security/coordinator.ts:227-263`) 创建一个独立 `AgentSession`：\n   - 使用触发扫描时的 active model（或 plan 指定的 model）。\n   - `spawns: \"security-reviewer\"` — 仅允许 spawn `security-reviewer` 子 agent。\n   - 追加 `securityCoordinatorPrompt` 系统提示，要求其通过内置 `task` 工具把文件审查任务分派给 `security-reviewer`。\n   - `enableLsp: true`、`lspReadOnly: true`；`enableMCP: false`。\n\n2. **security-reviewer（子 agent）**：`prompts/agents/security-reviewer.md` 定义其为只读安全专家，可用工具为 `read, grep, glob, lsp, ast_grep`。`task.agentModelOverrides` 强制把它映射到与 coordinator 相同的模型，`task.agentPrewalk` 强制设为 `\"off\"`。\n\n**结论**：`/security scan` 扫描的是 **当前仓库**（默认可通过 `--path`/`--diff` 等缩小范围）；执行者不是主会话 agent，而是一个 **专用 security coordinator 会话**，它再 spawn **security-reviewer 子 agent** 做实际的逐文件审查。\n", "details": {"resolvedPath": "/home/cpf/.omp/agent/sessions/-code-inside-agc/2026-09-05T13-12-07-926Z_01a071b2-a3f6-7445-9eb2-481f63f14c5e/FupModes.json", "contentType": "text/markdown", "meta": {"source": {"type": "internal", "value": "agent://FupModes?q=.report"}}}}