{"text": "# OMP v18.1.10 Slash 命令遗留细节补查\n\n## #20 /collab：WebSocket relay 默认指向与自建配置\n\n**结论**：默认指向官方托管 relay `wss://my.omp.sh`；用户**可以**配置自建 relay。  \n- 默认值定义在 `@oh-my-pi/pi-wire/src/index.ts:422`：`export const DEFAULT_RELAY_URL = \"wss://my.omp.sh\";`。  \n- 配置项为 `collab.relayUrl`，类型 string，默认值即 `DEFAULT_RELAY_URL`（`settings-schema.ts:2364-2372`）。  \n- `/collab` 启动时优先取用户传入的显式 URL，其次读 `ctx.settings.get(\"collab.relayUrl\")`，无配置则报错并提示设置（`builtin-collaboration.ts:347-350`）。  \n- 另有 `collab.webUrl` 可单独配置浏览器 UI 入口，留空时从 relayUrl 推导（`settings-schema.ts:2375-2385`）。  \n- 证据等级：源码实证。\n\n---\n\n## #14 /jobs：后台 job 的类型范围\n\n**结论**：jobs 快照里只会出现 **3 种** job 类型：`bash`、`task`、`eval`。  \n- 类型定义在 `async/job-manager.ts:53-54`：\n  ```ts\n  export type AsyncJobType = \"bash\" | \"task\" | \"eval\";\n  ```\n- `bash` — 长耗时 shell 命令（如 `bash` 工具自动后台化）。  \n- `task` — task/tan/vibe 等子 agent 派生（`task/tool.ts` 中注册为 `task` 类型）。  \n- `eval` — eval kernel 中的代码执行单元（`eval/handle-bridge.ts` 关联）。  \n- `/jobs` 命令通过 `session.getAsyncJobSnapshot({ recentLimit: 5 })` 读取 running + recent 列表，每行渲染 `[${job.type}]` 徽章（`builtin-session.ts:267-291`）。  \n- 证据等级：源码实证。\n\n---\n\n## #25 /review：1-16 个 reviewer 的构成与选出方式\n\n**结论**：reviewer 是**主 agent 通过 `task` 工具派生的子 agent（`agent: \"reviewer\"`）**，数量由 diff 统计启发式决定，上限 16。  \n- `/review` 是一个 bundled custom command，源码在 `extensibility/custom-commands/bundled/review/index.ts`。  \n- 数量计算函数 `getRecommendedAgentCount`（同文件 180-196 行）：\n  | diff 规模 | 推荐数量 |\n  |---|---|\n  | <100 行或 ≤2 文件 | 1 |\n  | <500 行 | min(2, 文件数) |\n  | <2000 行 | min(4, ⌈文件数/3⌉) |\n  | <5000 行 | min(8, ⌈文件数/2⌉) |\n  | ≥5000 行 | min(16, 文件数) |\n- 该数量被写入渲染后的 prompt 模板（`prompts/review-request.md`），主 agent 收到后按指引调用 `task` 工具，参数为 `agent: \"reviewer\"`、并给出 `tasks[]` 数组。  \n- prompt 原文：「Use the `task` tool with `agent: \"reviewer\"` and a `tasks` array.」以及分组建议（同目录/同模块/测试与实现放同一 agent）。  \n- 因此 reviewer 的**角色/模型**没有硬编码固定模型，而是走 OMP 标准的 `reviewer` agent role 路由（由用户配置或默认模型决定），**agent 类型为 task 子 agent**。  \n- 证据等级：源码实证。\n", "details": {"resolvedPath": "/home/cpf/.omp/agent/sessions/-code-inside-agc/2026-09-05T13-12-07-926Z_01a071b2-a3f6-7445-9eb2-481f63f14c5e/FupCollab.json", "contentType": "text/markdown", "meta": {"source": {"type": "internal", "value": "agent://FupCollab?q=.report"}}}}